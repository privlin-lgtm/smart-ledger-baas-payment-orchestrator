VITE_API_URL=http://localhost:8787
# Must match the API's own API_KEY (api/.env). Note this ends up in the browser bundle --
# it stops casual/drive-by discovery of the API, it is not a real per-user auth boundary.
VITE_API_KEY=
